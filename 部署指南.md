# 红运官网部署指南（阿里云 ECS + 宝塔）

当前项目不是纯静态站，而是三个运行单元：

- `dist/`：React 静态前端，由 Nginx 直接提供
- `apps/api`：Nitro BFF，仅监听 `127.0.0.1:3001`
- `apps/cms`：Strapi CMS，仅监听 `127.0.0.1:1337`
- MySQL：CMS 数据库，不开放公网

以下示例目录统一为 `/www/wwwroot/hongyun-react`，正式域名示例为 `www.example.com`，CMS 后台域名示例为 `cms.example.com`。上线前必须全部替换。

## 1. 上线前置条件

- 阿里云 ECS 建议 Ubuntu 22.04/24.04，至少 4 核 8 GB；CMS 构建阶段内存不足时临时增加 Swap。
- 宝塔安装 Nginx、MySQL 8、Node.js 22 LTS、PM2/Node 项目管理器。
- 安装系统工具：`git`、`clamav`、`mysql-client`、`sqlite3`、`tar`。
- 域名已解析；中国内地 ECS 对外提供网站前先完成 ICP 备案。
- 阿里云安全组仅公网开放 `80/443`；`22` 和宝塔面板端口只允许管理员固定 IP。不要开放 `1337/3001/3306`。
- 为主站和 CMS 后台分别申请 HTTPS 证书；Strapi 5 生产后台必须经可信 HTTPS 反代。

## 2. 上传代码与安装依赖

```bash
cd /www/wwwroot
git clone <你的仓库地址> hongyun-react
cd /www/wwwroot/hongyun-react

corepack enable
corepack prepare pnpm@9.15.9 --activate
pnpm install --frozen-lockfile
```

确认版本：

```bash
node --version
pnpm --version
```

Node 必须在项目声明的 `20–26` 范围内。

## 3. 创建 MySQL

在宝塔「数据库」中新建：

- 数据库名：`hongyun_cms`
- 用户名：使用独立低权限账号
- 字符集：`utf8mb4`
- 访问权限：仅本机

数据库密码不要出现在 Git、聊天、截图或部署日志中。

## 4. 生产环境变量

```bash
cp apps/cms/.env.example apps/cms/.env
cp apps/api/.env.example apps/api/.env
cp .env.example .env.production
chmod 600 apps/cms/.env apps/api/.env .env.production
```

编辑 `.env.production`：

```dotenv
VITE_MAPBOX_ACCESS_TOKEN=<限制为正式域名可用的 Mapbox 公共 Token>
VITE_API_BASE_URL=
```

同域部署时 `VITE_API_BASE_URL` 必须留空。Vite 变量会编译进浏览器包，不要在其中放任何服务端密钥。

编辑 `apps/cms/.env`，至少确认：

```dotenv
HOST=127.0.0.1
PORT=1337
PUBLIC_URL=https://cms.example.com
IS_PROXIED=true
SERVE_ADMIN=true
CORS_ORIGINS=https://cms.example.com,https://www.example.com

DATABASE_CLIENT=mysql
DATABASE_HOST=127.0.0.1
DATABASE_PORT=3306
DATABASE_NAME=hongyun_cms
DATABASE_USERNAME=<数据库账号>
DATABASE_PASSWORD=<强密码>
DATABASE_SSL=false

PRIVATE_UPLOAD_DIR=../api/.private-uploads
CLAMSCAN_BIN=clamscan
BFF_WEBHOOK_URL=http://127.0.0.1:3001/api/cms/webhook
CMS_WEBHOOK_SECRET=<与 API 完全相同的随机值>
```

`APP_KEYS`、`API_TOKEN_SALT`、`ADMIN_JWT_SECRET`、`TRANSFER_TOKEN_SALT`、`JWT_SECRET`、`ENCRYPTION_KEY` 必须全部替换为独立随机值。可逐项使用：

```bash
openssl rand -base64 48
```

编辑 `apps/api/.env`，至少确认：

```dotenv
NITRO_HOST=127.0.0.1
NITRO_PORT=3001
NITRO_STRAPI_URL=http://127.0.0.1:1337
NITRO_STRAPI_PUBLIC_URL=http://127.0.0.1:1337
NITRO_STRAPI_READ_TOKEN=
NITRO_STRAPI_WRITE_TOKEN=
NITRO_CMS_WEBHOOK_SECRET=<与 CMS 完全相同的随机值>
NITRO_FORM_ENCRYPTION_KEY=<openssl rand -hex 32 的结果>
NITRO_PRIVATE_UPLOAD_DIR=.private-uploads
NITRO_RESUME_RETENTION_DAYS=180
NITRO_RECRUITER_TOKEN=<独立随机值>
PUBLIC_SITE_URL=https://www.example.com
```

不要复制本地 `.env` 到生产。API 进程已配置启动时校验关键变量，缺失或仍为占位值会直接拒绝启动。

## 5. 初始化、构建与内容迁移

首次部署：

```bash
cd /www/wwwroot/hongyun-react
pnpm build:all
pnpm cms:seed
pnpm cms:tokens
pnpm cms:verify
```

说明：

- `cms:seed` 只补齐缺失的中文内容，不覆盖后台已编辑内容。
- `cms:tokens` 生成 BFF 只读 Token 和表单最小写权限 Token，并写入 `apps/api/.env`。
- 生产环境不要运行 `cms:seed:force`，除非已完成备份且明确要覆盖迁移内容。
- 英文接口保留，但英文正文未验收前不发布。

## 6. PM2 进程

在项目根目录执行：

```bash
pm2 start ecosystem.config.cjs
pm2 save
pm2 startup
```

按 `pm2 startup` 输出的命令再执行一次，使进程开机自启。

检查：

```bash
pm2 status
pm2 logs hongyun-cms --lines 100
pm2 logs hongyun-api --lines 100
curl -fsS http://127.0.0.1:3001/api/health
```

`ecosystem.config.cjs` 固定单实例运行 BFF，因为缓存和限流当前是进程内状态。扩容到多实例前必须先接入 Redis 等共享存储。

## 7. 宝塔 Nginx

1. 创建主站 `www.example.com`，根目录指向 `/www/wwwroot/hongyun-react/dist`。
2. 创建 CMS 站点 `cms.example.com`。
3. 两个站点都申请证书并开启 HTTPS。
4. 参考 [`docs/nginx-hongyun.conf.example`](docs/nginx-hongyun.conf.example) 配置反向代理。
5. 将示例中的 `203.0.113.10` 换成公司固定出口 IP 或 VPN 网段；不替换时 CMS 后台和招聘下载接口会被拒绝。
6. 执行 Nginx 配置检查后再重载：

```bash
nginx -t
```

必须满足：

- `/api/*` 和 `/sitemap.xml` → `127.0.0.1:3001`
- CMS 独立域名整站 → `127.0.0.1:1337`
- `/api/cms/webhook` 不允许公网访问
- `/api/admin/*` 只允许招聘人员固定 IP/VPN
- React 子路由使用 `try_files $uri $uri/ /index.html`

不要只反代 `/admin`；Strapi 后台还依赖 `/content-manager`、`/upload` 等路径。

## 8. 简历安全与定时任务

确认 ClamAV 可用：

```bash
clamscan --version
freshclam
```

PM2 配置已包含：

- 每 10 分钟扫描待处理简历
- 每天 03:15 清理到期简历和联系记录

简历仅保存在 `apps/api/.private-uploads`，目录不得放进 Nginx 静态根目录，也不得开放目录访问。

## 9. 备份

创建独立备份目录（最好挂载阿里云 OSS、快照盘或异地存储）：

```bash
mkdir -p /www/backup/hongyun
chmod 700 /www/backup/hongyun
./scripts/ops/backup-cms.sh /www/backup/hongyun
./scripts/ops/verify-backup.sh /www/backup/hongyun/hongyun-cms-YYYYMMDD-HHMMSS
```

在宝塔计划任务中每天执行备份，并设置保留周期。数据库、CMS 媒体、私有简历必须一起备份；`.env` 密钥应在独立密码库中保存，不要打进普通备份包。

首次上线前必须做一次恢复演练，确认 SQL、媒体和私有文件能还原。

## 10. 上线验收

```bash
pnpm check
pnpm --filter @hongyun/api typecheck
pnpm cms:verify
API_BASE_URL=https://www.example.com pnpm cms:verify:live
```

浏览器逐项检查：

- 首页、关于、产品分类/详情、解决方案、新闻、搜索、联系页
- 桌面和手机宽度；控制台无报错；关键请求无 4xx/5xx
- 技术咨询真实入库，失败时不会显示成功
- PDF/DOC/DOCX 简历真实入库、扫描后才可下载
- CMS 后台只有白名单/VPN 可访问，登录与媒体上传正常
- `https://www.example.com/sitemap.xml` 返回 XML
- 刷新任意 React 子路由不出现 404
- 外网无法连接 `1337/3001/3306`

## 11. 更新与回滚

更新前先备份，再执行：

```bash
cd /www/wwwroot/hongyun-react
git pull --ff-only
pnpm install --frozen-lockfile
pnpm check
pnpm build:all
pm2 reload ecosystem.config.cjs --update-env
curl -fsS http://127.0.0.1:3001/api/health
```

前端建议使用版本目录 + 软链接切换，避免删除旧 `dist` 后产生空窗。数据库变更回滚前必须先停写、备份当前数据，并使用已验证的备份恢复；不要用 `git reset --hard` 代替数据库回滚。

## 12. 阿里云与宝塔安全收口

- 安全组：公网只放行 80/443；SSH 和宝塔端口限固定 IP。
- 宝塔：修改默认面板端口和安全入口，开启双因素认证，定期更新面板/Nginx/MySQL。
- MySQL：仅监听本机或 VPC，不允许 `0.0.0.0/0`。
- 系统：开启安全更新、日志轮转、磁盘/内存/进程告警。
- 网站：主站强制 HTTPS；确认备案、隐私告知和简历/联系方式数据保存期限符合实际制度。

官方参考：

- 阿里云 ECS 安全组：https://help.aliyun.com/zh/ecs/user-guide/start-using-security-groups
- 宝塔反向代理：https://docs.bt.cn/user-guide/site/php/site-config/reverse-proxy
- Strapi 5 文档：https://docs.strapi.io/
